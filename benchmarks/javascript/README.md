# JavaScript / TypeScript benchmark corpus

Labeled fixtures for provider, agent, MCP, trust-boundary, privileged-operation, clean-negative, and adversarial-negative coverage.

Run the corpus with:

```bash
PYTHONPATH=src python -m aibom_inspector benchmark benchmarks/javascript/manifest.json
```
