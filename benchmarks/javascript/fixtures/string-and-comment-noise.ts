// AI-looking noise should not be a finding by itself.
const text = `Agent tools and process.env.SECRET are examples only.`;
const prompt = "client.responses.create({ model: 'example' })";
export { text, prompt };
