name: CI

on:
  push:
    branches: [ "main", "work" ]
  pull_request:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -e .[dev]
      - name: Check dependency consistency
        run: |
          python - <<'PY'
          import tomllib, sys, re
          py = tomllib.loads(open('pyproject.toml','rb').read())
          deps = {re.split(r'[<=>]', d.strip())[0].lower() for d in py.get('project', {}).get('dependencies', [])}
          reqs_lines = [l.strip() for l in open('requirements.txt').read().splitlines() if l.strip()]
          reqs = {re.split(r'[<=>]', r)[0].lower() for r in reqs_lines}
          missing = deps - reqs
          extra = reqs - deps
          if missing or extra:
              print('Dependency mismatch detected')
              if missing: print('in pyproject not in requirements:', missing)
              if extra: print('in requirements not in pyproject:', extra)
              sys.exit(1)
          print('Dependencies consistent')
          PY
      - name: Run ruff format
        run: ruff format --check .
      - name: Run ruff
        run: ruff check .
      - name: Run mypy
        run: mypy .
      - name: Check for bidirectional control characters
        run: python -m aibom_inspector.bidi_guard

  tests:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.10", "3.11", "3.12"]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -e .[dev]
      - name: Run tests
        run: pytest -q

  scale-runtime:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install package
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -e .[dev]
      - name: Run production runtime regression tests
        run: pytest -q tests/test_production_runtime.py tests/test_relationship_store.py tests/test_audit_log.py
      - name: Run scale benchmark smoke test
        run: python benchmarks/scale_benchmark.py --artifacts 250 --bytes-per-artifact 65536 --workers 4

  rust:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        working-directory: crates/core
    steps:
      - uses: actions/checkout@v4
      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - name: Cargo fmt
        run: cargo fmt -- --check
      - name: Cargo clippy
        run: cargo clippy --all-targets --all-features -- -D warnings
      - name: Cargo test
        run: cargo test --all-targets
