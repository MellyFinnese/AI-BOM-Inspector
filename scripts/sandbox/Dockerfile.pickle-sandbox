FROM python:3.12-slim

WORKDIR /app

# Copy repository into image (read-only use in runtime)
COPY . /app

# Keep runtime light - no heavy installs required for the read-only tracer
RUN python -m pip install --no-cache-dir --upgrade pip

# Default entrypoint expects a python -c command
ENTRYPOINT ["python", "-u", "-c"]
