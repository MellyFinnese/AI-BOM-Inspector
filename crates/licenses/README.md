# Licenses crate

Placeholder for Rust helpers that normalize licenses, map SPDX identifiers, or enforce policy decisions around copyleft/permissive licensing. Keeping this crate separate makes it easier to ship a focused library or WASM module once the implementation is ready.
