# Advisories crate

Reserved for adapters that pull CVE and advisory data (e.g., OSV, NVD, vendor feeds). This crate will eventually expose a clean API the Python front-end can call into for enrichment when `--online` is enabled.
