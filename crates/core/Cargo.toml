[package]
name = "tensor_fuzz"
version = "0.1.0"
edition = "2021"

[lib]
name = "tensor_fuzz"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.24", features = ["extension-module"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
half = "2.3"
